# Client-side overrides (optional)
NEXT_PUBLIC_TIP_URL=
STEAM_APP_ID_DOW_DE=

# Supabase seeding (required for scripts/seed-leaderboards.mjs)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional tuning for the snapshot seeding script
LEADERBOARD_SNAPSHOT_SOURCE=manual-initial-seed
LEADERBOARD_SNAPSHOT_MAX=200
LEADERBOARD_PAGE_SIZE=100

# Optional tuning for the player enrichment script
ENRICH_PLAYER_LIMIT=400
ENRICH_CONCURRENCY=4
ENRICH_RELIC_DELAY_MS=300
ENRICH_RELIC_REQUEST_CAP=10000
ENRICH_ALIAS_FALLBACK=true
ENRICH_INCLUDE_PERSONAL_STATS=false
