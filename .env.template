# Client-side overrides (optional)
NEXT_PUBLIC_TIP_URL=
STEAM_APP_ID=

# Supabase configuration
SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional tuning for the snapshot seeding script
LEADERBOARD_SNAPSHOT_SOURCE=manual-initial-seed
LEADERBOARD_SNAPSHOT_MAX=200
LEADERBOARD_PAGE_SIZE=100

# Optional tuning for the player enrichment script (OPTIMIZED FOR 25K PLAYERS)
ENRICH_PLAYER_LIMIT=2000
ENRICH_CONCURRENCY=10
ENRICH_RELIC_DELAY_MS=100
ENRICH_RELIC_REQUEST_CAP=50000
ENRICH_ALIAS_FALLBACK=false
ENRICH_INCLUDE_PERSONAL_STATS=false

# Speed optimizations for crawler
CRAWL_RELIC_DELAY_MS=100
CRAWL_RELIC_REQUEST_CAP=50000
CRAWL_COOLDOWN_MINUTES=60
CRAWL_UPSERT_CHUNK_SIZE=500
CRAWL_IDLE_SLEEP_MS=1000

# Concurrent crawler settings (for crawl-player-matches-concurrent.mjs)
CRAWL_CONCURRENCY=3
CRAWL_BATCH_SIZE=10

# XP refresh crawler settings (scripts/crawl-player-xp-refresh.mjs)
XP_REFRESH_BATCH_SIZE=1000
XP_REFRESH_CONCURRENCY=6
XP_REFRESH_MATCH_COUNT=20
XP_REFRESH_UPSERT_CHUNK=500
XP_REFRESH_RELIC_DELAY_MS=150
XP_REFRESH_RELIC_REQUEST_CAP=20000
XP_REFRESH_LOG_EVERY=100
